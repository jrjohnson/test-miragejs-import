import dostuff from 'dummy/utils/dostuff';
import { module, test } from 'qunit';

module('Unit | Utility | dostuff', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = dostuff();
    assert.ok(result);
  });
});
